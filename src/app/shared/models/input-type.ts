export enum InputType {
  Text = 'text',
  Date = 'date',
  Number = 'number',
  Custom = 'custom',
  Select = 'select',
  Address = 'address',
  Email = 'email',
  Phone = 'phone',
  Image = 'image'
}
